# Scripts

This directory is intentionally empty in the base toolkit.

Implementation skills generate use-case-specific scripts for the user to review and execute.

Reusable scripts that become stable team assets may be committed here after review, but should:
- accept configuration rather than hard-coded customer values
- contain no credentials or secrets
- be deterministic where appropriate
- include validation/dry-run behavior where practical
- be documented and code reviewed
